import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app6.aula',
  appName: 'app6',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
